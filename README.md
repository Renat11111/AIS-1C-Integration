# AIS-1C Integration Proxy Service

Высокопроизводительный сервис-шлюз для асинхронной и гарантированной интеграции между **AIS** (Automated Information System) и **1С:Предприятие**.

Сервис работает по принципу надежной очереди: принимает данные от AIS, мгновенно сохраняет их на диск и в фоновом режиме отправляет в 1С, сглаживая пиковые нагрузки и обеспечивая сохранность данных при сбоях.

## 🚀 Ключевые возможности

1.  **Гарантированная доставка (Persistent Queue):**
    *   Входящие запросы сохраняются во встроенную БД (**PocketBase** / SQLite) перед обработкой.
    *   Данные переживают перезагрузку сервера или сбой питания.
    *   Удаление из очереди происходит **только** после подтверждения успеха (HTTP 200) от 1С.

2.  **Архитектура и Производительность:**
    *   **Worker Pool:** Настраиваемое количество параллельных потоков отправки.
    *   **Smart Dispatcher:** Обработка данных пачками (Batch Processing) для снижения нагрузки на БД.
    *   **Защита от сбоев JSON:** Надежная обработка "битых" данных в очереди — они изолируются и не блокируют работу конвейера.
    *   **Retry Policy:** Экспоненциальная задержка (Exponential Backoff) при недоступности 1С.

3.  **Безопасность и Наблюдаемость:**
    *   **API Key Auth:** Защита от несанкционированного доступа с защитой от тайминг-атак.
    *   **Prometheus Metrics:** Встроенный экспортер метрик (глубина очереди, скорость обработки, ошибки).
    *   **Structured Logging:** JSON-логирование (Zerolog) для удобного парсинга.

---

## 🛠 Технический стек

*   **Язык:** Go 1.25+
*   **База данных:** PocketBase (Embedded)
*   **Транспорт:** HTTP/REST
*   **Конфигурация:** `.env`

---

## 📦 Установка и Запуск

### Предварительные требования
*   Windows / Linux сервер
*   Доступ к портам 8081 (API) и 3000 (Grafana, опционально)

### Развертывание (Production)

1.  **Подготовка файлов:**
    Скопируйте содержимое папки `build/` на целевой сервер.

2.  **Настройка:**
    Создайте файл `.env` рядом с исполняемым файлом (используйте `.env.example` как шаблон):
    ```ini
    SERVER_PORT=:8081
    AIS_TOKEN=your_secret_token_here
    
    # Настройки 1С
    ONEC_BASE_URL=http://onec-server/base/hs/ais/v1/data
    ONEC_USER=admin
    ONEC_PASSWORD=secret
    
    # Тюнинг производительности
    WORKER_COUNT=5
    BATCH_SIZE=50
    ```

3.  **Запуск:**
    *   `start.bat` — запускает сервер с автоматическим перезапуском при сбоях.
    *   `setup.bat` — (опционально) устанавливает и настраивает Prometheus + Grafana для мониторинга.

### Разработка (Development)

```bash
# Клонирование репозитория
git clone https://github.com/Renat11111/AIS-1C-Integration.git

# Установка зависимостей
go mod download

# Запуск
go run cmd/api/main.go
```

---

## 💻 API Интерфейсы

| Сервис | Адрес | Описание |
| :--- | :--- | :--- |
| **Data Endpoint** | `POST http://localhost:8081/api/v1/data` | Основной метод приема данных (Продажи, Возвраты) |
| **Swagger UI** | `http://localhost:8081/swagger/index.html` | Интерактивная документация API |
| **Admin UI** | `http://localhost:8081/_/` | Панель управления PocketBase (просмотр очереди) |
| **Metrics** | `http://localhost:8081/metrics` | Метрики для Prometheus |

### Пример запроса (cURL)

```bash
curl -X POST "http://localhost:8081/api/v1/data" \
     -H "Content-Type: application/json" \
     -H "X-API-Key: your_secret_token_here" \
     -d 
{
           "id": "req-12345",
           "method": "sale",
           "data": {
             "sale_id": "SALE-001",
             "date": "2025-12-22T10:00:00Z",
             "sum": 1500.00
           }
         }
```

---

## 📊 Мониторинг

Проект включает готовую конфигурацию для мониторинга.
При использовании `setup.bat`, Grafana будет доступна по адресу `http://localhost:3000`.

**Ключевые метрики:**
*   `ais_queue_depth` — Текущее количество задач в очереди.
*   `ais_worker_duration_seconds` — Время обработки одного запроса.
*   `ais_processed_total` — Счетчик успешных/ошибочных отправок.

---

## ⚠️ Текущий статус

> **Важно:** Интеграция с 1С находится в режиме заглушки (Stub).
> Сервис принимает данные, валидирует их и ставит в очередь, но вместо реальной отправки имитирует задержку и успешный ответ.
> Это сделано намеренно до готовности API со стороны 1С.