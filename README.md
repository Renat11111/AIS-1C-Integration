# AIS-1C Integration Proxy Service

Высокопроизводительный сервис-шлюз для асинхронной и гарантированной интеграции между **AIS** (Automated Information System) и **1С:Предприятие**.

Сервис работает по принципу надежной очереди: принимает данные от AIS, мгновенно сохраняет их на диск и в фоновом режиме отправляет в 1С (пакетами), сглаживая пиковые нагрузки и обеспечивая сохранность данных при сбоях.

## 🚀 Ключевые возможности

1.  **Гарантированная доставка (Persistent Queue):**
    *   Входящие запросы сохраняются во встроенную БД (**PocketBase** / SQLite) перед обработкой.
    *   Данные переживают перезагрузку сервера или сбой питания.
    *   Удаление из очереди происходит **только** после подтверждения успеха (HTTP 200) от 1С.

2.  **Dead Letter Queue (DLQ) и Умные повторы:**
    *   **Статусы задач:** `pending` (в очереди), `processing` (в работе), `retry` (ожидает повтора), `failed` (ошибка).
    *   **Exponential Backoff:** При ошибках 1С интервал повтора растет экспоненциально (от 1 минуты до 24 часов).
    *   **Circuit Breaker:** Автоматическая пауза отправки при массовых сбоях на стороне 1С.
    *   **Изоляция сбоев:** "Битые" задачи (после N попыток) перемещаются в статус `failed` и не блокируют очередь.

3.  **Архитектура и Производительность:**
    *   **Worker Pool:** Настраиваемое количество параллельных потоков отправки.
    *   **Batch Processing:** Данные отправляются в 1С пакетами (Batch) для снижения накладных расходов HTTP.
    *   **Точность данных:** Использование `json.Number` гарантирует корректную передачу длинных ID и точных сумм без потери точности.
    *   **Graceful Shutdown:** При остановке сервис дожидается завершения текущих отправок.

4.  **Безопасность и Наблюдаемость:**
    *   **API Key Auth:** Защита по токену в заголовке `X-API-Key`.
    *   **Prometheus Metrics:** Детальные метрики состояния системы.
    *   **Structured Logging:** Логирование в JSON (Zerolog) с автоматической ротацией файлов.

---

## 🛠 Технический стек

*   **Язык:** Go 1.25+
*   **База данных:** PocketBase v0.35+ (Embedded)
*   **Транспорт:** HTTP/REST (JSON)
*   **Мониторинг:** Prometheus + Grafana

---

## 📦 Установка и Запуск

### Предварительные требования
*   Windows / Linux сервер
*   Доступ к портам 8081 (API) и 3000 (Grafana, опционально)

### Развертывание (Production)

1.  **Подготовка файлов:**
    Скопируйте содержимое папки `build/` на целевой сервер.

2.  **Настройка:**
    Создайте файл `.env` рядом с исполняемым файлом (используйте `.env.example` как шаблон):
    ```ini
    SERVER_HOST=127.0.0.1:8081
    AIS_TOKEN=your_secret_token_here
    
    # Настройки 1С
    ONEC_BASE_URL=http://onec-server/base/hs/ais/v1/data
    ONEC_USER=admin
    ONEC_PASSWORD=secret
    
    # Тюнинг производительности
    WORKER_COUNT=5
    BATCH_SIZE=50
    ```

3.  **Запуск:**
    *   `start.bat` — запускает сервер с автоматическим перезапуском при сбоях.
    *   `setup.bat` — (опционально) устанавливает и настраивает Prometheus + Grafana для мониторинга.

### Разработка (Development)

```bash
# Клонирование репозитория
git clone https://github.com/Renat11111/AIS-1C-Integration.git

# Установка зависимостей
go mod download

# Запуск
go run cmd/api/main.go
```

---

## 💻 API Интерфейсы

| Эндпоинт | Метод | Описание |
| :--- | :--- | :--- |
| `/api/v1/data` | **POST** | Прием новой продажи или обновления |
| `/api/v1/data` | **DELETE** | Отмена продажи (требует `SalePayStatusId`) |
| `/api/v1/health` | **GET** | Проверка статуса, версии и времени сборки |
| `/api/v1/queue/retry-failed` | **POST** | Перезапуск задач со статусом `failed` |
| `/swagger/index.html` | GET | Интерактивная документация Swagger |
| `/_/` | GET | Панель управления PocketBase (Admin UI) |

### Пример: Продажа (POST)
```json
{
  "id": "req-unique-uuid",
  "data": {
    "SaleId": "12345678901234567",
    "SaleMainAmount": 150.50,
    "SaleDetails": [...]
  }
}
```

### Пример: Отмена (DELETE)
```json
{
  "id": "req-cancel-uuid",
  "data": {
    "SaleId": "12345678901234567",
    "SalePayStatusId": 4
  }
}
```

---

## 📊 Мониторинг и Обсерваемость

Проект включает в себя полноценный стек мониторинга, который разворачивается автоматически при запуске `setup.bat`.

### Доступ к инструментам:
| Сервис | Адрес | Доступ (По умолчанию) |
| :--- | :--- | :--- |
| **Grafana** | `http://localhost:3000` | Логин: `admin`, Пароль: `admin` |
| **Prometheus** | `http://localhost:9090` | Просмотр сырых данных и алертов |
| **Метрики API** | `http://localhost:8081/metrics` | Эндпоинт для сбора данных |

### Основные метрики:
*   `ais_queue_depth` — Текущее количество задач в очереди (`pending` + `retry`). Позволяет оценить нагрузку и отставание системы.
*   `ais_worker_duration_seconds` — Время, затраченное воркером на одну попытку отправки в 1С. Помогает выявить проблемы с производительностью на стороне 1С.
*   `ais_processed_total` — Общий счетчик обработанных запросов. Группируется по меткам:
    *   `status`: `success` (доставлено) или `error` (ошибка).
    *   `method`: `POST`, `PUT`, `DELETE`.

Дашборд **"AIS Integration"** автоматически импортируется в Grafana при первом запуске, отображая графики в реальном времени.

---

## ⚠️ Текущий статус

Сервис полностью готов к эксплуатации (Production Ready).
Реализован полноценный HTTP-клиент для взаимодействия с API 1С.
Включен механизм **Circuit Breaker** и **Batch Processing** для максимальной надежности.
